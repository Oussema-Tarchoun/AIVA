{% extends 'base.html.twig' %}

{% block title %}{{ repas.nom }}{% endblock %}

{% block body %}
<!-- Sidebar -->
<aside class="sidebar" id="sidebar">
    <div class="sidebar-header">
        <div class="logo">
            <img src="{{ asset('images/logo.png') }}" alt="Logo"
                 style="width: 80px; display: block; margin: 12px auto;">
        </div>
        <span class="logo-text">EnergyDash</span>
    </div>

    <ul class="nav-menu">

        {# Menu Principal #}
        <li class="nav-section">
            <span class="nav-section-title">Menu Principal</span>
            <ul>
                <li class="nav-item">
                    <a href="{{ path('dashboard') }}"
                       class="nav-link {% if app.request.attributes.get('_route') == 'dashboard' %}active{% endif %}">
                        <i class="fa-solid fa-house"></i> Tableau de bord
                    </a>
                </li>
            </ul>
        </li>

        {# Menu énergies #}
        <li class="nav-section">
            <span class="nav-section-title">Menu énergies</span>
            <ul>
                <li class="nav-item">
                    <a href="{{ path('energy') }}"
                       class="nav-link {% if app.request.attributes.get('_route') == 'energy' %}active{% endif %}">
                        <i class="fa-solid fa-bolt"></i> Consommation d'énergie
                    </a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('recommendations') }}"
                       class="nav-link {% if app.request.attributes.get('_route') == 'recommendations' %}active{% endif %}">
                        <i class="fa-solid fa-lightbulb"></i> Recommandations
                    </a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('stateenergi') }}"
                       class="nav-link {% if app.request.attributes.get('_route') == 'stateenergi' %}active{% endif %}">
                        <i class="fa-solid fa-chart-line"></i> Statistiques d'énergie
                    </a>
                </li>
            </ul>
        </li>

        {# ✅ Menu Nutrition (Aliments + Repas) #}
        <li class="nav-section">
            <span class="nav-section-title">Nutrition</span>
            <ul>
                <li class="nav-item">
                    <a href="{{ path('app_aliment_index') }}"
                       class="nav-link {% if app.request.attributes.get('_route') starts with 'app_aliment_' %}active{% endif %}">
                        <i class="fa-solid fa-apple-whole"></i> Aliments
                    </a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('app_repas_index') }}"
                       class="nav-link {% if app.request.attributes.get('_route') starts with 'repas_aliment_' %}active{% endif %}">
                        <i class="fa-solid fa-utensils"></i> Repas
                    </a>
                </li>
            </ul>
        </li>

        {# ✅ Menu Formation (Cours/Chapitres) #}
        <li class="nav-section">
            <span class="nav-section-title">Formation</span>
            <ul>
                <li class="nav-item">
                    <a href="{{ path('app_cours_index') }}"
                       class="nav-link {% if app.request.attributes.get('_route') starts with 'app_cours_' %}active{% endif %}">
                        <i class="fa-solid fa-book"></i> Cours
                    </a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('app_chapitre_index') }}"
                       class="nav-link {% if app.request.attributes.get('_route') starts with 'app_chapitre_' %}active{% endif %}">
                        <i class="fa-solid fa-list"></i> Chapitres
                    </a>
                </li>
            </ul>
        </li>

        {# Compte #}
        <li class="nav-section">
            <span class="nav-section-title">Compte</span>
            <ul>
                <li class="nav-item">
                    <a href="{{ path('app_profile') }}"
                       class="nav-link {% if app.request.attributes.get('_route') == 'app_profile' %}active{% endif %}">
                        <i class="fa-solid fa-user"></i> Profil
                    </a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('app_user_index') }}"
                       class="nav-link {% if app.request.attributes.get('_route') starts with 'app_user_' %}active{% endif %}">
                        <i class="fa-solid fa-users"></i> Users
                    </a>
                </li>

                <li class="nav-item">
                    <a href="{{ path('app_logout') }}" class="nav-link">
                        <i class="fa-solid fa-right-from-bracket"></i> Déconnexion
                    </a>
                </li>
            </ul>
        </li>

    </ul>
</aside>
<div class="main-content">
    <nav class="navbar">
        <div class="page-header">
            <h1 class="page-title">{{ repas.nom }}</h1>
            <div class="page-breadcrumb">
                <a href="{{ path('dashboard') }}">Dashboard</a>
                <span>/</span>
                <a href="{{ path('app_repas_index') }}">Repas</a>
                <span>/</span>
                <span>Détails</span>
            </div>
        </div>
        <div class="navbar-right">
            <a href="{{ path('app_repas_edit', {'id': repas.id}) }}" class="btn btn-primary" style="width: auto; padding: 10px 20px; margin-right: 12px;">
                <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="margin-right: 8px;">
                    <path d="M11 4H4a2 2 0 0 0-2 2v14a2 2 0 0 0 2 2h14a2 2 0 0 0 2-2v-7"/>
                    <path d="M18.5 2.5a2.121 2.121 0 0 1 3 3L12 15l-4 1 1-4 9.5-9.5z"/>
                </svg>
                Modifier
            </a>
            <a href="{{ path('app_repas_index') }}" class="btn btn-secondary" style="width: auto; padding: 10px 20px;">
                Retour
            </a>
            
        </div>
    </nav>

    <div class="settings-grid">
        <div class="glass-card settings-nav-card">
            <div style="padding: 20px;">
                <div class="profile-avatar-large" style="width: 100px; height: 100px; font-size: 36px; margin: 0 auto 20px;">
                    <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" style="width: 50px; height: 50px;">
                        <path d="M3 2v7c0 1.1.9 2 2 2h4a2 2 0 0 0 2-2V2"/>
                        <path d="M7 2v20"/>
                        <path d="M21 15V2v0a5 5 0 0 0-5 5v6c0 1.1.9 2 2 2h3Zm0 0v7"/>
                    </svg>
                </div>
                <h3 style="text-align: center; margin-bottom: 8px;">{{ repas.nom }}</h3>
                <p style="text-align: center; color: var(--text-muted); font-size: 14px; text-transform: capitalize;">{{ repas.type|replace({'-': ' '}) }}</p>
                
                <div style="margin-top: 24px; padding-top: 24px; border-top: 1px solid var(--glass-border);">
                    <div style="display: flex; justify-content: space-between; margin-bottom: 12px;">
                        <span style="color: var(--text-muted); font-size: 14px;">Heure</span>
                        <span style="font-weight: 500;">{{ repas.heure ? repas.heure|date('H:i') : 'N/A' }}</span>
                    </div>
                    <div style="display: flex; justify-content: space-between; margin-bottom: 12px;">
                        <span style="color: var(--text-muted); font-size: 14px;">Calories</span>
                        <span style="font-weight: 500; color: var(--emerald);">{{ repas.calories }} kcal</span>
                    </div>
                    <div style="display: flex; justify-content: space-between; margin-bottom: 12px;">
                        <span style="color: var(--text-muted); font-size: 14px;">Aliments</span>
                        <span style="font-weight: 500;">{{ repas.aliments|length }}</span>
                    </div>
                </div>
                
<div style="margin-top: 24px;">
    <form id="delete-form" method="post" action="{{ path('app_repas_delete', {'id': repas.id}) }}" onsubmit="return confirm('Êtes-vous sûr de vouloir supprimer ce repas ?');">
        <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ repas.id) }}">
        <button type="submit" class="btn" style="width: 100%; margin-top: 20px; background: rgba(239, 68, 68, 0.15); color: #ef4444;">
            Supprimer
        </button>
    </form>
</div>
            </div>
        </div>

        <div class="glass-card">
            {% if repas.description %}
            <div class="settings-section">
                <h3 class="settings-section-title">Description</h3>
                <p style="color: var(--text-muted); line-height: 1.6;">{{ repas.description }}</p>
            </div>
            {% endif %}

            {% if repas.aliments|length > 0 %}
            <div class="settings-section">
                <h3 class="settings-section-title">Aliments Inclus ({{ repas.aliments|length }})</h3>
                
                <div class="table-wrapper" style="margin: 0;">
                    <table class="data-table" style="min-width: 100%;">
                        <thead>
                            <tr>
                                <th>Aliment</th>
                                <th>Quantité</th>
                            </tr>
                        </thead>
                        <tbody>
                            {% for aliment in repas.aliments %}
                            <tr>
                                <td><strong>{{ aliment.nom }}</strong></td>
                                <td>{{ aliment.quantite|default('N/A') }}</td>
                            </tr>
                            {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
            {% else %}
            <div class="settings-section">
                <p style="color: var(--text-muted); text-align: center; padding: 24px;">
                    Aucun aliment associé à ce repas.
                </p>
            </div>
            {% endif %}
        </div>
    </div>
</div>
{% endblock %}